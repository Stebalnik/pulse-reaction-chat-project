# Research pipeline

Planned offline tooling for paper ingestion support, dataset manifests, benchmark runs, and reproducible reports.

This package must not retain raw video or biometric time series by default. Research data handling requires explicit consent, retention limits, and documented deletion behavior.
