# Desktop client

Planned first launch surface for a packaged desktop beta.

Development defaults:

- local port: `1059`
- app origin: `http://localhost:1059`

The first desktop client should support camera consent, pause, WebRTC roulette-style chat, local signal-quality feedback, and baseline-relative physiological trend display. It must not expose raw video or biometric-adjacent time series by default.
